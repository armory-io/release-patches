apiVersion: spinnaker.armory.io/v1alpha2
kind: SpinnakerService
metadata:
  name: spinnaker
spec:
  spinnakerConfig:
    config:
      persistentStorage:
        persistentStoreType: s3
        s3:
          bucket: '${ SPIN_BUCKET }'
          rootFolder: '${parameters.namespace}'